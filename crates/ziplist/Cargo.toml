[package]
name = "ziplist"
version = "0.1.0"
description = "Compact byte-format collections codec for cache storage"

edition = { workspace = true }
repository = { workspace = true }
license = { workspace = true }
